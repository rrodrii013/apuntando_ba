¿Tendré que hacer scripts con JavaScript para las funciones?


1. En el html, debo crear una función .onclick que se despliegue un form(o similar) con las los inputs de coordenadas y demás.
2. El método de puntería no elegido debe desaparecer o dirigir a un nuevo html con la info que solicita al usuario que está en la logica de py. Se prosigue a seleccionar y completar cada campo solicitado. Al final del mismo habrá un botón para enviar todos los datos. *NO SE ENVIARÁ UNO POR UNO COMO SUCEDE AHORA* 
3. El botón para enviar los datos ejecutará una función la cual realiza los cálculos del método de puntería mostrando los resultados del mismo. En esta ocasión los datos brindados por el usuario también aparecerán. Acá estaría bueno hacer una función que tarde un par de segundos en calcular para que quede vistoso y luego lance toda la info como si fuese una ventana nueva. 